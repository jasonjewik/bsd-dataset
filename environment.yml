channels:
  - defaults
dependencies:
  - pip
  - pip:
    - cdsapi
    - configobj
    - numpy
    - pandas
    - regex
    - scikit-image
    - torch
    - torchvision
    - tqdm
    - wandb
    - wget
    - xarray
    - einops
