channels:
  - defaults
dependencies:
  - pip
  - pip:
    - attrdict
    - cdsapi
    - configobj
    - dask
    - numpy
    - pandas
    - regex
    - scikit-image
    - torch
    - torchvision
    - tqdm
    - wandb
    - wget
    - xarray
    - einops